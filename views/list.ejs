<!DOCTYPE html>
<html>
    <head>
        <Title>TODO List</Title>
        <link rel="stylesheet" href="styles.css">
    </head>
    <body>
        <div class="box">
            <h1>TODO List</h1>
        </div>

        <div class="box">
           
            <% ejesItems.forEach(function(items) { %>
                <form action="/delete" method="POST">
                    <div class="item" >
                        <input type="checkbox" name="checkbox1" value="<%= items._id %>" onchange="this.form.submit()">
                        <p><%= items.name %></p> 
                    </div>
                </form>
                
               <%  }) %>
            
            <form action="/" method="POST" class="item">
                <input type="text" name="element1" placeholder="Enter the task">
                <button type="submit">+</button>
            </form>

        </div>
    </body>
    <script>
    //    function myFunction() {
    //         var checkBox =  document.getElementById("checkB");
    //         var item =  document.getElementById("listItem");

    //         if(checkBox.checked == true) {
    //             item.style.textDecoration = "line-through";
    //         } else {
    //             item.style.textDecoration = "none";
    //         }
    //     }
    </script>
</html>